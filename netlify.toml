[build]
  command = "npm run build"
  publish = "dist"

# Single Page Application (SPA) Routing
# This ensures that any refresh or direct URL navigation
# falls back to your index.html so React Router takes over.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200